name: 🚀 SSH Deploy to Websupport

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Hugo site with Docker
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/src \
            -w /src \
            klakegg/hugo:ext-alpine \
            --minify
      - name: List contents of public/
        run: |
          ls -lah public || echo "⚠️ public/ priečinok neexistuje"
      - name: Upload via SSH (rsync)
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 28144
          script: |
            rsync -avz --delete public/ /home/uid2787266/tomashujcak.sk/sub/hugo/
