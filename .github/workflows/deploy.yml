name: 🐛 Debug Hugo Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout repository
        uses: actions/checkout@v4

      - name: 🛠️ Build Hugo site with Docker
        run: |
          echo ">> Spúšťam Hugo build"
          docker run --rm \
            -v ${{ github.workspace }}:/src \
            -w /src \
            klakegg/hugo:ext-alpine \
            --minify
          echo "✅ Hugo build hotový"

      - name: 📂 Skontroluj obsah priečinku public/
        run: |
          echo ">> Obsah priečinka ./public:"
          ls -lah public || echo "⚠️ Priečinok public NEEXISTUJE!"

      - name: 🔐 Pripoj sa cez SSH a over existenciu cieľovej zložky
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 28144
          script: |
            echo ">> Aktuálny priečinok:"
            pwd
            echo ">> Obsah ~/tomashujcak.sk/sub:"
            ls -lah ~/tomashujcak.sk/sub || echo "❌ Cesta neexistuje"

      - name: 🚀 Deploy Hugo cez rsync
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 28144
          script: |
            echo ">> Deployujem public/ na server..."
            rsync -avz --delete public/ ~/tomashujcak.sk/sub/hugo/ || echo "❌ RSYNC zlyhal!"
